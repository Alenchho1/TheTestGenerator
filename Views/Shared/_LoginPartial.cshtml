@using Microsoft.AspNetCore.Identity
@using TestGenerator.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a class="nav-link user-greeting" asp-area="Identity" asp-page="/Account/Manage/Index" title="Настройки">
            <i class="bi bi-person-circle"></i> Здравей, @((await UserManager.GetUserAsync(User))?.FirstName ?? User.Identity?.Name)!
        </a>
    </li>
    <li class="nav-item">
        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button type="submit" class="nav-link btn btn-link text-light">
                <i class="bi bi-box-arrow-right"></i> Изход
            </button>
        </form>
    </li>
}
</ul>
